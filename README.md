# SecureApps-ToDoList
Secure apps project
setup and configuration

this is a php project that requires the following to work:
php 8
SQLite3
local server.

to see the web app via a local server as i did install XAMPP and place the folder containing all the code in the htdocs to view via localhost
while not nessary having DBbrowser can help to see the changes into the database
